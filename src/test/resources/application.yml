spring:
  kafka:
    consumer:
      auto-offset-reset: earliest
      group-id: bankwithmint
  jpa:
    hibernate:
      ddl-auto: create-drop
    database-platform: org.hibernate.dialect.H2Dialect
  datasource:
    url: jdbc:h2:mem:testdb
    username: test
    password: test123
    driver-class-name: org.h2.Driver
test:
  topic: embedded-test-topic

listen:
  auto:
    start: true


